apiVersion: v1
kind: Service
metadata:
  name: {{ include "learning-api.fullname" . }}
  labels:
    {{- include "learning-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "learning-api.selectorLabels" . | nindent 4 }}
